<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.demo.boot.mapper.RoleMapper">
    <resultMap id="BaseResultMap" type="com.demo.boot.entity.Role">
        <result column="id" property="id" jdbcType="INTEGER"/>
        <result column="rolename" property="rolename" jdbcType="VARCHAR"/>
    </resultMap>
    <sql id="Base_Column_List">
        id, rolename
    </sql>
    <select id="getRolesByUser" resultMap="BaseResultMap" parameterType="java.lang.Integer">
        select
        <include refid="Base_Column_List"/>
        from t_role r,t_user_role ur
        where r.id = ur.role_id AND ur.role_id = #{id,jdbcType=INTEGER}
    </select>
    <insert id="insert" parameterType="com.demo.boot.entity.Role">
    insert into t_role (id, rolename)
    values (#{id,jdbcType=INTEGER}, #{rolename,jdbcType=VARCHAR})
  </insert>
    <insert id="insertSelective" parameterType="com.demo.boot.entity.Role">
        insert into t_role
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">
                id,
            </if>
            <if test="rolename != null">
                rolename,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">
                #{id,jdbcType=INTEGER},
            </if>
            <if test="rolename != null">
                #{rolename,jdbcType=VARCHAR},
            </if>
        </trim>
    </insert>
</mapper>